import { createStore } from 'vuex'

// State
const state = {
    
  // Array to store custom post types of 'menu'
  menuPostsData: [],
  
};

export default createStore({
  state,
  mutations,
  actions,
})
